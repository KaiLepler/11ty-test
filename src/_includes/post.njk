{% extends "layout.njk" %}

{% block content %}
  <article>
    <h1>{{ title }}</h1>
    <p><time datetime="{{ date | readableDate('yyyy-MM-dd') }}">{{ date | readableDate }}</time></p>
    {{ content | safe }}
  </article>
{% endblock %}
